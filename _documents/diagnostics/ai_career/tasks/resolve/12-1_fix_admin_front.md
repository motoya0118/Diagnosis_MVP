# 実施すること
## TODO
### 1. ヘッダーを作る
共通コンポーネント化する
ログアウト、dashbordへ遷移するメニューを付与

### 2. テストをtestディレクトリに切り分ける
- `admin_front/tests`を作成して、ここにテストコードを配置する
- 移行対象：
  - admin_front/hooksの配下
  - admin_front/libの配下

### 3. セキュリティ向上
#### 3-1. next_authによるJWTに変更
- 現状の問題点：
  - 独自実装になっていてセキュリティ的に弱い
- 解決方針：
  - next_authを使用する(`frontend`を参照)
    - userテーブルは`admin_users`を参照する(現状のまま)

#### 3-2. バックエンドAPIの認証
- 現状の問題点:
  - `backend/app/routers/admin_diagnostics.py`が一切認証を確認していないので誰でもコールできてしまう
- 解決方針:
  - 全APIはjwtを引数に受けてユーザー認証を実施する(共通処理化する)
  - 存在しないユーザーは弾いて、存在するユーザーのみリクセストを受け付ける挙動になっているかテストを追加する